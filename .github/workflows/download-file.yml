name: Download File

on:
  push:
    branches:
      - main  # Cambia esto según la rama que estés utilizando

jobs:
  download:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'  # Usa la versión de Python que necesites

      - name: Install requests
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Download file
        run: python downloads.py
        
      - name: Commit downloaded file
        run: |
          git config --local user.name "github-actions"
          git config --local user.email "github-actions@github.com"
          git add *.xml.gz  # Asegúrate de que coincida con la extensión de los archivos que descargas
          git commit -m "Add downloaded files" || echo "No changes to commit"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Este token es proporcionado automáticamente por GitHub Actions
